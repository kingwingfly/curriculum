[package]
name = "curriculum"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", optional = true, features = ["derive"] }
chrono = { version = "0.4" }
bon = "3.7.1"
getset = "0.1.6"
variadics_please = "1.1"

[features]
default = []
serde = ["dep:serde"]
